-- https://github.com/projekt0n/github-nvim-theme
require('github-theme').setup({
    options = {
        transparent = true -- Disable setting backgroun
    }
})

vim.cmd([[colorscheme github_dark]])
